# Vendored third-party library source (reference checkout only, not part of this project)
/jsmpp/

# Ballerina build output/cache
target/
*.balo

# Gradle build output/cache. The wrapper itself (gradlew, gradlew.bat, gradle/wrapper/*)
# is NOT ignored — it's committed so `./gradlew build` works with no local Gradle install.
.gradle/
smpp/build/
smpp/native/build/
smpp/ballerina/build/
smpp/ballerina/lib/

# Compiled Java classes / logs from manual test artifacts
mock-smsc/*.class
*.log
