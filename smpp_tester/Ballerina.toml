[package]
org = "ramith"
name = "smpp_tester"
version = "0.1.0"
distribution = "2201.13.0"

# Consume the SMPP connector from the local repository
# (published via `bal push --repository=local` in ../smpp).
[[dependency]]
org = "ramith"
name = "smpp"
version = "0.1.0"
repository = "local"
